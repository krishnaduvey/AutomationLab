namespace AutomationLab.Common.Pages.UI
{
    /// <summary>
    /// Base abstraction for all UI pages.
    /// Pages contain ONLY locators and element definitions.
    /// No waits. No execution logic.
    /// </summary>
    public abstract class BasePage
    {
        // Intentionally empty.
        // Pages are pure abstraction containers.
    }
}
